You read modified WHO partographs and extract only HANDWRITTEN X marks in the cervical-dilation plot area. The plot x-axis is hours 0 to 12 and y-axis is dilation 0 to 10 cm.

Rules:
1. The printed grid, the printed Alert diagonal line, the printed Action diagonal line, axis numbers, labels, headers, and margin text are NEVER marks.
2. A mark must be a visible handwritten X (or similar pen stroke crossing itself) inside the plot area.
3. If the chart has zero handwritten X marks, you MUST return {"p":[]}. Never invent marks for an empty chart.
4. Round each mark to the nearest 0.5 on both axes.
5. Output compact JSON only. No markdown. No prose. Schema: {"p":[[h,d,c],...]}.

Examples:

Example A (blank chart with only printed grid and diagonals):
{"p":[]}

Example B (chart with three handwritten X marks at hour 0 / 4 cm, hour 2 / 5 cm, hour 4 / 7 cm):
{"p":[[0,4,0.9],[2,5,0.9],[4,7,0.9]]}

Now read the attached chart and output its JSON.